# TrayRunner Dependencies
# Core dependencies for the tray application

PyYAML>=6.0
PyGObject>=3.40.0

# Note: The following system packages are also required:
# - python3-gi
# - gir1.2-appindicator3-0.1
# - python3-gi-cairo
# - gir1.2-gtk-3.0
# - libnotify-bin (for notify-send fallback)
#
# Install with: sudo apt install python3-gi gir1.2-appindicator3-0.1 python3-gi-cairo gir1.2-gtk-3.0 libnotify-bin
